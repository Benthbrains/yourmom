[package]
name = "yourmom"
version = "0.1.0"

[dependencies]

[runner]
# Choose one: "python", "node", "rust", "static", etc.
type = "node"

[build]
# Command used to build your app
command = "npm install && npm run build"

[run]
# Command used to start your app
command = "npm start"
